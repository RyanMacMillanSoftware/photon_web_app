// Place all the styles related to the TimePunch controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

textstatus{
	font: bold 14px arial, sans-serif;
	text-align: center;
	display: inline;
	margin-left: 5px;
	position: relative;
  	top: -2px;
}

btn-logout{
	position: absolute;
	left: 300px;
}