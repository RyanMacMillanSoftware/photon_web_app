web: bundle exec puma -C config/puma.rb
#We also need to make a so-called Procfile
# to tell Heroku to run a Puma process in production,
# as shown in Listing 7.38. The Procfile should be created 
#in your application’s root directory (i.e., in the same location
# as the Gemfile).